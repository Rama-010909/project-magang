{
  "buildCommand": "npm run build",
  "outputDirectory": "dist",
  "installCommand": "npm install",
  "functions": {
    "api/monitor.js": {
      "maxDuration": 60
    }
  },
  "crons": [
    {
      "path": "/api/monitor",
      "schedule": "* * * * *"
    }
  ]
}
