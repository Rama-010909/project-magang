rules_version = '2';
service cloud.firestore {
  match /databases/{database}/documents {
    match /assets/{id} { allow read, write: if true; }
    match /maintenance/{id} { allow read, write: if true; }
  }
}
